{
  "config": {
    "step": {
      "user": {
        "title": "BMS Intercom",
        "description": "Choose how to set up the intercom.",
        "menu_options": {
          "demo": "Demo mode (virtual panel)",
          "real": "Real panel"
        }
      },
      "demo": {
        "title": "Demo mode",
        "description": "A virtual intercom for testing without hardware.",
        "data": {
          "name": "Name"
        }
      },
      "real": {
        "title": "Connect to the panel",
        "data": {
          "name": "Name",
          "host": "Panel IP address",
          "username": "Username",
          "password": "Password",
          "http_port": "HTTP port (ISAPI)",
          "rtsp_port": "RTSP port",
          "door_no": "Door relay number",
          "channel": "Video channel (101 = main stream, 102 = sub stream)",
          "use_alert_stream": "Use the event stream (alertStream)"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not reach the panel. Check IP, port and credentials.",
      "invalid_auth": "The panel rejected the credentials (401). Check the login and that the user has ISAPI rights."
    },
    "abort": {
      "already_configured": "This panel is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "BMS Intercom settings",
        "description": "The browser microphone only works over HTTPS; the integration starts a local HTTPS endpoint automatically. You can also change the panel video channel, turn the event stream off and set the call reset timeout here.",
        "data": {
          "proxy_port": "Built-in HTTPS port (default 8443)",
          "https_url": "External HTTPS address (optional)",
          "call_timeout": "Reset the call after, seconds",
          "channel": "Video channel (101 = main stream, 102 = sub stream)",
          "use_alert_stream": "Use the event stream (alertStream)"
        }
      }
    },
    "error": {
      "https_required": "The address must start with https://"
    }
  }
}
